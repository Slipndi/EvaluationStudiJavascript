const colors = require('tailwindcss/colors'); 

module.exports = {
  content: ["**.html"],
  theme: {
    colors: {
      primary: colors.slate,
      secondary: colors.neutral,
      white: colors.white,
      black: colors.black,
      red: colors.red,
    }
  },
  plugins: [],
}
