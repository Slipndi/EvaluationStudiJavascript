const colors = require('tailwindcss/colors'); 

module.exports = {
  content: ["**.html"],
  theme: {
    colors: {
      primary: colors.red,
      secondary: colors.orange,
      white: colors.white,
      black: colors.black
    }
  },
  plugins: [],
}
